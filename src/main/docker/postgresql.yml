version: '2'
services:
  mediaservice-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/MediaService/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=MediaService
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
